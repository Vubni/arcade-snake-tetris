"""Константы игры"""

CELL_SIZE = 30
GRID_WIDTH = 15  # Ширина поля в клетках
GRID_HEIGHT = 25  # Высота поля в клетках
MARGIN = 50  # Отступы от краев окна

SCREEN_WIDTH = GRID_WIDTH * CELL_SIZE + MARGIN * 2
SCREEN_HEIGHT = GRID_HEIGHT * CELL_SIZE + MARGIN * 2

# Настройки сложности (скорость падения фигур, скорость движения змейки, задержка спавна)
DIFFICULTY_SETTINGS = {
    'easy': {
        'fall_speed': 0.16,    # Медленнее падают
        'snake_speed': 0.20,   # Медленнее движется
        'spawn_delay': 1.0     # Больше времени на реакцию
    },
    'medium': {
        'fall_speed': 0.12,     # Средняя скорость
        'snake_speed': 0.18,
        'spawn_delay': 0.7      # Средняя задержка
    },
    'hard': {
        'fall_speed': 0.085,    # Быстрее падают (меньшее приращение)
        'snake_speed': 0.16,    # Быстрее движется (меньшее приращение)
        'spawn_delay': 0.5      # Меньше времени на реакцию
    }
}


COLORS = [
    (0, 255, 255),      # Яркий циан для I
    (0, 100, 255),      # Яркий синий для J
    (255, 140, 0),      # Яркий оранжевый для L
    (255, 255, 0),      # Яркий желтый для O
    (50, 205, 50),      # Яркий зеленый для S
    (186, 85, 211),     # Яркий фиолетовый для T
    (255, 50, 50),      # Яркий красный для Z
]

TETROMINOES = {
    'I': [(-1, 0), (0, 0), (1, 0), (2, 0)],
    'J': [(-1, -1), (-1, 0), (0, 0), (1, 0)],
    'L': [(1, -1), (-1, 0), (0, 0), (1, 0)],
    'O': [(0, 0), (1, 0), (0, 1), (1, 1)],
    'S': [(-1, 0), (0, 0), (0, 1), (1, 1)],
    'T': [(0, -1), (-1, 0), (0, 0), (1, 0)],
    'Z': [(-1, 1), (0, 1), (0, 0), (1, 0)],
}

# Задержка после появления новой фигуры (в секундах)
# Теперь используется из DIFFICULTY_SETTINGS, это значение по умолчанию
PIECE_SPAWN_DELAY = 0.5
# Альтернативно: задержка в циклах (если > 0, используется вместо времени)
PIECE_SPAWN_DELAY_CYCLES = 0  # 0 = использовать время, 1-2 = использовать циклы

# Количество блоков в столбце, при котором столбец уничтожается
COLUMN_CLEAR_THRESHOLD = 10

# Высота, при которой ряд уничтожается (GRID_HEIGHT - 4 означает, что осталось только 4 свободных клетки сверху)
ROW_CLEAR_HEIGHT_THRESHOLD = GRID_HEIGHT - 6

# Очки за каждую очищенную линию в тетрисе
POINTS_PER_LINE = 100
